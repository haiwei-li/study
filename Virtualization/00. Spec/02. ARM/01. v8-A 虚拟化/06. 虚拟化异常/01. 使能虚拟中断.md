为发起虚拟中断到 EL0/1,hypervisor 必须设置 HCR_EL2 中的相关位. 比如, 为使能 vIRQ 发送, hypervisor 必须设置 HCR_EL2.IMO. 这个设置路由物理 IRQ 异常到 EL2, 并使能虚拟异常的发起到 EL1.

根据每种中断类型控制虚拟中断. 在理论上, 虚拟机可以配置为接受物理 FIQ 和虚拟 IRQ. 特别是这并不常见. 虚拟机通常被配置为只接受虚拟中断.
