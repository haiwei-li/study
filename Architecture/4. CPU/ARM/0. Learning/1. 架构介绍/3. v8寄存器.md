- [1. 通用寄存器](#1-通用寄存器)
- [2. 处理器状态寄存器](#2-处理器状态寄存器)
- [3. 特殊寄存器](#3-特殊寄存器)
- [4. 系统寄存器](#4-系统寄存器)
- [5. 总结](#5-总结)
  - [5.1. 每个EL一套的寄存器](#51-每个el一套的寄存器)
    - [5.1.1. 保存程序状态寄存器 (Saved Program Status Register, SPSR)](#511-保存程序状态寄存器-saved-program-status-register-spsr)
    - [5.1.2. 异常链接寄存器 (Exception Link Register, ELR)](#512-异常链接寄存器-exception-link-register-elr)
    - [5.1.3. 栈指针 (Stack Pointer, SP)](#513-栈指针-stack-pointer-sp)
    - [5.1.4. 部分系统控制寄存器](#514-部分系统控制寄存器)
  - [5.2. 共享的寄存器](#52-共享的寄存器)
    - [5.2.1. 通用寄存器 (General Purpose Registers, GPRs)](#521-通用寄存器-general-purpose-registers-gprs)
    - [5.2.2. 程序计数器 (Program Counter, PC)](#522-程序计数器-program-counter-pc)
    - [5.2.3. 当前程序状态寄存器 (Processor State, PSTATE)](#523-当前程序状态寄存器-processor-state-pstate)

# 1. 通用寄存器

> 在所有异常级别(EL0 ~ EL3)是共享的

AArch64 执行状态支持 31 个 64 位的通用寄存器, 分别是 X0～X30 寄存器, 而 AArch32 状态支持 16 个 32 位的通用寄存器.

除用于数据运算和存储之外, 通用寄存器还可以在函数调用过程中起到特殊作用, ARM64 体系结构的函数调用标准和规范对此有所约定, 如下图所示.

![2025-01-25-11-47-05.png](./images/2025-01-25-11-47-05.png)

在 AArch64 状态下, 使用 X(如 X0, X30 等)表示 64 位通用寄存器. 另外, 还可以使用 W 来表示低 32 位的数据, 如 W0 表示 X0 寄存器的低 32 位数据, W1 表示 X1 寄存器的低 32 位数据. 如下图.

![2025-01-25-11-47-46.png](./images/2025-01-25-11-47-46.png)

# 2. 处理器状态寄存器

> 在所有异常级别(EL0 ~ EL3)是共享的

AArch64 体系结构使用 **PSTATE** 寄存器来表示当前处理器状态(processor state).

![2025-01-25-12-02-26.png](./images/2025-01-25-12-02-26.png)

# 3. 特殊寄存器

ARMv8 体系结构除支持 31 个通用寄存器之外, 还提供多个特殊的寄存器

![2025-01-25-12-03-11.png](./images/2025-01-25-12-03-11.png)

1) **零寄存器**

ARMv8 体系结构提供**两个零寄存器**(zero register), 这些寄存器的内容全是 0, 可以用作源寄存器, 也可以用作目标寄存器. WZR 是 32 位的零寄存器, XZR 是 64 位的零寄存器.

2) **PC (Program Counter) 指针寄存器**

> 在所有异常级别(EL0 ~ EL3)是共享的

PC 指针寄存器通常用来指向当前运行指令的下一条指令的地址, 用于控制程序中指令的运行顺序, 但是编程人员不能通过指令来直接访问它.

3) **SP 寄存器**

> 每个 EL 一套该寄存器

ARMv8 体系结构支持 4 个异常等级, **每一个异常等级**都有一个专门的 SP 寄存器 `SP_ELn` , 如处理器运行在 EL1 时选择 `SP_EL1` 寄存器作为 SP 寄存器.

* `SP_EL0`: EL0 下的 SP 寄存器.

* `SP_EL1`: EL1 下的 SP 寄存器.

* `SP_EL2`: EL2 下的 SP 寄存器.

* `SP_EL3`: EL3 下的 SP 寄存器.

当处理器运行在**比 EL0 高**的异常等级时, 处理器可以访问如下寄存器.

* 当前异常等级对应的 SP 寄存器 `SP_ELn` .

* **EL0** 对应的 SP 寄存器 `SP_EL0` 可以当作一个临时寄存器, 如 Linux 内核使用该寄存器存放进程中 task_struct 数据结构的指针.

当处理器运行在 EL0 时, 它只能访问 `SP_EL0`, 而不能访问其他高级的 SP 寄存器.

4) **备份程序状态寄存器**

> 每个 EL 一套该寄存器

当我们运行一个异常处理程序时, 处理器的备份程序会保存到**备份程序状态寄存器** (`Saved Program Status Register`, SPSR) 里. 当异常将要发生时, 处理器会把 PSTATE 寄存器的值暂时保存到 SPSR 里; 当异常处理完成并返回时, 再把 SPSR 的值恢复到 PSTATE 寄存器.

SPSR 的格式如下图所示.

![2025-01-25-12-04-25.png](./images/2025-01-25-12-04-25.png)

SPSR 的重要字段如下表所示.

![2025-01-25-12-05-01.png](./images/2025-01-25-12-05-01.png)

5) ELR

> 每个 EL 一套该寄存器

ELR 存放了**异常返回地址**. 异常处理完成后, 程序会从 ELR 指向的地址继续执行。

1) CurrentEL 寄存器

该寄存器表示 PSTATE 寄存器中的 EL 字段, 其中保存了**当前异常等级**. 使用 MRS 指令可以读取当前异常等级.

* 0: 表示 EL0.

* 1: 表示 EL1.

* 2: 表示 EL2.

* 3: 表示 EL3.

7) DAIF 寄存器

该寄存器表示 PSTATE 寄存器中的 { D , A , I , F } 字段.

8) SPSel 寄存器

该寄存器表示 PSTATE 寄存器中的 SP 字段, 用于在 SP_EL0 和 `SP_EL n` 中选择 SP 寄存器.

9) PAN 寄存器

PAN 寄存器表示 PSTATE 寄存器中的 PAN(Privileged Access Never, 特权禁止访问)字段. 可以通过 MSR 和 MRS 指令来设置 PAN 寄存器. 当**内核态**拥有访问用户态内存或者执行用户态程序的能力时, 攻击者就可以利用漏洞轻松地执行用户的恶意程序. 为了修复这个漏洞, 在 ARMv8.1 中新增了 PAN 特性, **防止内核态恶意访问用户态内存**. 如果内核态需要访问用户态内存, 那么需要主动调用内核提供的接口, 例如 `copy_from_user()` 或者 `copy_to_user()` 函数.

PAN 寄存器的值如下.

* 0: 表示在内核态可以访问用户态内存.

* 1: 表示在内核态访问用户态内存会触发一个**访问权限异常**.

10) UAO 寄存器

该寄存器表示 PSTATE 寄存器中的 UAO(User Access Override, 用户访问覆盖)字段. 我们可以通过 MSR 和 MRS 指令设置 UAO 寄存器. UAO 为 1 表示在 EL1 和 EL2 执行这非特权指令 (例如 LDTR, STTR) 的效果与特权指令 (例如 LDR, STR) 是一样的.

11) NZCV 寄存器

该寄存器表示 PSTATE 寄存器中的 { N , Z , C , V } 字段.

# 4. 系统寄存器

ARMv8 体系结构还定义了很多的系统寄存器, 通过访问和设置这些系统寄存器来完成对处理器不同的功能配置.

* 在 ARMv7 体系结构中, 我们需要通过访问 CP15 协处理器来间接访问这些系统寄存器;

* 而在 ARMv8 体系结构中没有协处理器, 可直接访问系统寄存器.

ARMv8 体系结构支持如下 7 类系统寄存器:

* 通用系统控制寄存器;

* 调试寄存器;

* 性能监控寄存器;

* 活动监控寄存器;

* 统计扩展寄存器;

* RAS 寄存器;

* 通用定时器寄存器.

系统寄存器支持不同的异常等级的访问, 通常系统寄存器会使用 "`Reg_ELn`" 的方式来表示.

* `Reg_EL1`: 处理器处于 EL1, EL2 以及 EL3 时可以访问该寄存器.

* `Reg_EL2`: 处理器处于 EL2 和 EL3 时可以访问该寄存器.

* 大部分系统寄存器不支持处理器处于 EL0 时访问, 但也有一些例外, 如 `CTR_EL0`.

程序可以通过 MSR 和 MRS 指令访问系统寄存器.

```
mrs X0, TTBR0_EL1    // 把 TTBR0_EL1 的值复制到 X0 寄存器
msr TTBR0_EL1, X0    // 把 X0 寄存器的值复制到 TTBR0_EL1
```

# 5. 总结

在AArch64架构中, 不同类型的寄存器在异常级别 (EL) 间的分配情况不同, 下面详细介绍哪些是每个EL一套, 哪些是共享的:

## 5.1. 每个EL一套的寄存器

### 5.1.1. 保存程序状态寄存器 (Saved Program Status Register, SPSR)

- **作用**: 当发生异常时, 当前的程序状态会被保存到SPSR中, 异常处理完成后再从SPSR恢复到CPSR (当前程序状态寄存器) .

- **说明**: 每个异常级别 (EL1、EL2、EL3) 都有自己的SPSR, 分别为SPSR_EL1、SPSR_EL2、SPSR_EL3. 例如, 当从EL0进入EL1处理异常时, EL0的CPSR内容会被保存到SPSR_EL1中.

### 5.1.2. 异常链接寄存器 (Exception Link Register, ELR)

- **作用**: 用于保存异常发生时的返回地址, 异常处理完成后, 程序会从ELR指向的地址继续执行.

- **说明**: 每个异常级别都有对应的ELR, 如ELR_EL1、ELR_EL2、ELR_EL3. 以系统调用异常为例, 当从用户态 (EL0) 陷入内核态 (EL1) 时, 当前指令地址会被保存到ELR_EL1中, 系统调用处理完成后, 会从ELR_EL1指向的地址返回用户态程序继续执行.

### 5.1.3. 栈指针 (Stack Pointer, SP)

- **作用**: 指向当前栈的顶部, 用于管理函数调用栈, 保存局部变量和函数调用信息.

- **说明**: 除了通用的SP_EL0供EL0使用外, EL1、EL2、EL3各有独立的栈指针. 例如, 在EL1运行的内核代码使用自己独立的栈, 由SP_EL1指向, 这样不同异常级别可以独立管理自己的栈空间, 避免栈数据的混乱.

### 5.1.4. 部分系统控制寄存器

- **作用**: 这些寄存器用于控制和配置系统的各种功能, 如内存管理、中断处理、虚拟化等.

- **说明**: 不同异常级别有独立的系统控制寄存器集合. 例如, 在EL1有用于管理内存映射和访问权限的寄存器 (如TTBR0_EL1、TTBR1_EL1等); EL2有用于虚拟化相关的控制寄存器, 以支持虚拟机的运行; EL3有用于安全监控模式的控制寄存器, 确保系统的安全隔离.

## 5.2. 共享的寄存器

### 5.2.1. 通用寄存器 (General Purpose Registers, GPRs)

- **范围**: R0 - R30.

- **作用**: 用于存储临时数据、函数参数和返回值等.

- **说明**: 这些寄存器在所有异常级别 (EL0 - EL3) 中是共享的. 例如, 在用户程序 (EL0) 和内核代码 (EL1) 之间进行上下文切换时, 通用寄存器的内容可以被保存和恢复, 以保证程序执行的连续性.

### 5.2.2. 程序计数器 (Program Counter, PC)

- **作用**: 指示当前正在执行的指令地址.

- **说明**: 虽然在异常发生时PC会根据异常处理流程进行更新, 但它在概念上是所有异常级别共享的, 代表着程序执行的位置.

### 5.2.3. 当前程序状态寄存器 (Processor State, PSTATE)

- **作用**: 反映当前处理器的状态, 如条件标志位、中断使能状态等.

- **说明**: CPSR在任何时刻都反映当前执行代码所在异常级别的状态, 不同异常级别通过保存和恢复CPSR来维护各自的执行状态.
